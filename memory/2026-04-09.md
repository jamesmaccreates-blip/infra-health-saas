### Update to confirm `mcd` skill installation
The user apparently requested installing 'mcd' but the last skill commands installed `web-search` and `github`. Record that the skill installation workflow works, and note that 'mcd' remains pending.
